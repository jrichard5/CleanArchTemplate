{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    //Now using the secret manager. I realize I cannot use this in production.  I would get rid of this line, but I still want it for template purposes.
    //"TheConnection": "server=(localdb)\\MSSQLLocalDb;database=NonDefaultDb;trusted_connection=true;"
  }
}
